# Production build — no hard-coded backend URL.
# Users configure their local backend address in the app settings panel,
# which saves it to localStorage as "sentioApiUrl".
# runtimeConfig.ts picks it up from there at runtime.
VITE_API_BASE_URL=
VITE_BRAIN_STREAM_URL=
